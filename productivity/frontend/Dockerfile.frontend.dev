# Dockerfile.frontend.dev — только для docker-compose.local.yml
# Запускает vite dev server с hot-reload
FROM node:20-alpine

WORKDIR /app

COPY package*.json ./
RUN npm ci

# Не копируем src — он монтируется через volume
EXPOSE 5173

CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]